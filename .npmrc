# Force isolated installation - never share with other projects
package-lock=true
save-exact=false

# Ensure clean installs
prefer-offline=false
fund=false
audit=true

# Prevent cache pollution between projects
# Each npm install runs in this project's context only
